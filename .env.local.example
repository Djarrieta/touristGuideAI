# Environment variables for Tourist Guide
# Copy this file to .env.local and fill in the values.
# Note: Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.

# Google Maps JavaScript API key (required)
# Enable the "Maps JavaScript API" in Google Cloud and restrict the key to HTTP referrers.
# Dev server runs at http://localhost:3005 by default.

NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key-here
QDRANT_API_KEY=your-qdrant-api-key-here
